# 静默启动 · 首次打开加速 — 产品决策

> 记录时间: 2026-07-02  
> 状态: **已关闭**（不再扩展本主题下的其他技术路线）

## 结论

在 **不预暖 WebView、静默启动仅托盘** 的前提下，已通过 **方案 E（缩短冷启动）** 落地优化；**其余方案一律不做**。

| 方案 | 说明 | 决策 |
|------|------|------|
| **E** | 首屏即时渲染、shell 分级 IPC、设置页懒加载、分块构建、UI 就绪后再 show 新主窗 | ✅ **已执行**（commit `34f843a`） |
| **A** | 静默可选预暖隐藏主窗 | ❌ 不做（Windows 易破坏静默；已回退预暖） |
| **B** | 托盘 hover / 按下意图预暖 | ❌ 不做 |
| **C** | 双窗 / warmup 标签 | ❌ 不做 |
| **D** | 原生壳 + 后台 WebView | ❌ 不做 |
| **F** | 仅 Release 预暖、Dev 不预暖 | ❌ 不做 |

## 与本文档夹内 `execution-plan.md` 的关系

`execution-plan.md` 描述的是 **方案 A（主窗口预暖）**，曾短暂实现后因静默失效已撤销。**不得以该文档为准继续开发**；仅作历史讨论备查。

## 当前产品行为（维护基线）

- 静默启动：不自动创建主窗（`create: false` + 开发配置合并已修复）。
- 首次点托盘：冷启动 WebView，经方案 E 优化后尽快出 UI。
- 再次点托盘：`hide` 保留实例，打开较快。
- 不再引入「静默预暖」或额外隐藏主窗实例。

## 验收

Release 安装包（`二向贴_0.1.0_x64-setup.exe`）下 **静默启动 + 首次托盘打开** 体感 **尚可**（2026-07-02），满足当前目标；不再推进预暖等其它方案。
