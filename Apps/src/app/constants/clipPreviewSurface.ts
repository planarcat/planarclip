import { SURFACE_REVEAL_BG } from "./surfaceReveal";

/** List-row content block; matches `ClipImagePreview` button surface (border + surface reveal hover). */
export const CLIP_LIST_PREVIEW_SURFACE = `w-full overflow-hidden rounded-lg border border-border bg-secondary/30 text-left ${SURFACE_REVEAL_BG} hover:border-primary/40 hover:bg-secondary/50`;
